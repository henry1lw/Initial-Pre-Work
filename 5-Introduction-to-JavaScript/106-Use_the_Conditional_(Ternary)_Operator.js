<!--Learned to use the Conditional (Ternary) Operator.-->
<--Again, sytax and specific operators are warranted.-->

function checkEqual(a, b) {
 return (a === b ? true : false );
}

checkEqual(1, 2);
