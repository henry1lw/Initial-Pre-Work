<!--Learned to use multiple Conditional (Ternary) Operators.-->
<!--That you must be practiced and up to speed on word problems.-->
<!--Patience and perserverance are critical.-->

function checkSign(num) {
 return (num === 0 ) ? "zero" : (num > 0) ? "positive" : "negative";
}

checkSign(10);
